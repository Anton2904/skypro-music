## Skypro Music Next.js

Проект инициализирован через `create-next-app` с TypeScript и App Router.

### Запуск

```bash
npm install
npm run dev
```

Приложение будет доступно на `http://localhost:3000`.
